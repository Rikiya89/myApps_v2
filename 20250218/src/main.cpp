#include "ofMain.h"
#include "ofApp.h"

//========================================================================
int main(){
    // Set the window size to 1080 x 1350 and launch the app
    ofSetupOpenGL(720, 1280, OF_WINDOW);
    ofRunApp(new ofApp());
}
